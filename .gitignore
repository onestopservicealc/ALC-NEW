
# โฟลเดอร์ชั่วคราวของ npm run check:imports
.import-check-*

# ค่าลับ — ห้ามขึ้น git
.env
.env.local
.env.*.local

# ไฟล์ SQL ที่มีค่าลับ สร้างจาก npm run setup:cron
pg-cron-setup.sql

# ไฟล์สำรองก่อนล้างข้อมูล มีข้อมูลจริง ห้ามขึ้น git
backup-before-cleanup-*.json
